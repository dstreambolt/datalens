╔══════════════════════════════════════════════════════════════╗
║            🚀 DStreamBolt Quick Reference Card              ║
╚══════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ONE-CLICK DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Without DNS (Fastest - Ready in 10 min):
  cd terraform
  ./deploy.sh

With DNS (Custom domain):
  cd terraform
  ./deploy.sh --with-dns
  # Then add DNS CNAME record shown in output

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
VERIFY DEPLOYMENT
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Health Check (All Services):
  cd terraform
  ./verify.sh

View Outputs:
  terraform output
  terraform output alb_url
  terraform output -json credentials

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
ACCESS URLs (Replace <alb-dns> with your ALB DNS)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Via ALB (Always works):
  Landing:       https://<alb-dns>/
  Jenkins:       https://<alb-dns>/jenkins
  Grafana:       https://<alb-dns>/grafana
  Kafka Manager: https://<alb-dns>/kafkamgr
  Spark UI:      https://<alb-dns>/spark
  Ingestion:     https://<alb-dns>/ingest

Via Domain (After DNS setup):
  https://dstreambolt.click/
  https://jenkins.dstreambolt.click/
  https://grafana.dstreambolt.click/
  https://kafkamgr.dstreambolt.click/
  https://spark.dstreambolt.click/
  https://ingest.dstreambolt.click/

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SSH ACCESS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Get IPs:
  terraform output -json direct_access

SSH to DevOps:
  ssh -i ~/dstreambolt-access-key.pem ubuntu@<devops-ip>

SSH to Spark:
  ssh -i ~/dstreambolt-access-key.pem ubuntu@<spark-ip>

SSH to Kafka (via DevOps):
  ssh -i ~/dstreambolt-access-key.pem ubuntu@<devops-ip>
  ssh -i ~/dstreambolt-access-key.pem ubuntu@<kafka-ip>

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
DEFAULT CREDENTIALS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Get all credentials:
  terraform output -json credentials

Jenkins:
  User: admin
  Pass: terraform output jenkins_password

Grafana:
  User: admin
  Pass: terraform output grafana_password

MySQL:
  User: root
  Pass: terraform output mysql_root_password

AKHQ/Kafka Manager:
  User: admin
  Pass: DStreamBolt2025!

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Services Not Starting:
  ssh ubuntu@<ip>
  tail -100 /var/log/cloud-init-output.log
  sudo journalctl -u <service-name> -f

Check Service Status:
  systemctl status jenkins
  systemctl status grafana-server
  systemctl status kafka
  systemctl status dstreambolt-ingest

Restart Service:
  sudo systemctl restart <service-name>

View Logs:
  sudo journalctl -u <service-name> -n 100

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MANUAL OPERATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Redeploy Infrastructure:
  cd terraform
  terraform plan
  terraform apply

Destroy Everything:
  cd terraform
  terraform destroy
  # WARNING: Deletes all data!

Setup DNS/SSL (Optional):
  cd scripts
  ./setup_domain_ssl.sh

Test Kafka Topics:
  cd scripts
  ./test_kafka_topics.sh

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
KAFKA OPERATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

List Topics (via AKHQ):
  https://<alb-dns>/kafkamgr

Or SSH to Kafka:
  PRIVATE_IP=$(hostname -I | awk '{print $1}')
  /opt/kafka/bin/kafka-topics.sh --list \
    --bootstrap-server $PRIVATE_IP:9092

Create Topic:
  /opt/kafka/bin/kafka-topics.sh --create \
    --bootstrap-server $PRIVATE_IP:9092 \
    --replication-factor 1 \
    --partitions 3 \
    --topic my-topic

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
SPARK OPERATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Spark Master UI:
  https://<alb-dns>/spark

Submit Job (SSH to Spark Master):
  PRIVATE_IP=$(hostname -I | awk '{print $1}')
  /opt/spark/bin/spark-submit \
    --master spark://$PRIVATE_IP:7077 \
    --deploy-mode client \
    your-job.py

Check Spark Logs:
  tail -f /opt/spark/logs/spark-*.out

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MYSQL OPERATIONS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Connect to MySQL (SSH to DevOps):
  sudo mysql -u root

Or with password:
  mysql -u root -p
  # Enter password from: terraform output mysql_root_password

List Databases:
  SHOW DATABASES;

Use DStreamBolt DB:
  USE dstreambolt_metrics;
  SHOW TABLES;

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
MONITORING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Grafana Dashboards:
  https://<alb-dns>/grafana
  Login: admin / <grafana-password>

View Metrics:
  - DStreamBolt Overview
  - Kafka Metrics
  - Spark Jobs
  - System Resources

Import Dashboard:
  Upload JSON from: grafana/customer-analytics-dashboard.json

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
CI/CD WITH JENKINS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Jenkins UI:
  https://<alb-dns>/jenkins
  Login: admin / <jenkins-password>

Setup GitHub Credentials:
  cd scripts
  ./setup_jenkins_github.sh

Deploy Spark Jobs:
  - Job: DStreamBolt-Deploy-Spark-Scala
  - Builds from: release/v1.0.0

Deploy Ingestion:
  - Job: DStreamBolt-Deploy-Ingestion
  - Updates: dstreambolt-ingest nodes

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
FILES & DOCUMENTATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Deployment Guide:
  ONE_CLICK_DEPLOYMENT.md

SSL Setup:
  SSL_CERTIFICATE_SETUP.md

Architecture:
  README.md

Observability:
  observability/README.md

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
COST OPTIMIZATION
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Estimated Monthly Cost:
  - t3.small (3x): ~$15/month
  - t3.micro (2x): ~$6/month
  - ALB: ~$16/month
  - Data transfer: varies
  Total: ~$40-50/month

Stop When Not in Use:
  aws ec2 stop-instances \
    --instance-ids <id1> <id2> \
    --region ap-south-1

Start Again:
  aws ec2 start-instances \
    --instance-ids <id1> <id2> \
    --region ap-south-1

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
NEED HELP?
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

1. Run: ./verify.sh
2. Check: /var/log/cloud-init-output.log
3. View: sudo journalctl -u <service> -f
4. Read: ONE_CLICK_DEPLOYMENT.md

Common Issues:
  - Wait 10 min for services to start
  - Check security groups if can't connect
  - Verify SSH key permissions: chmod 600 ~/dstreambolt-access-key.pem
  - Run terraform refresh if outputs stale

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Print this reference:
  cat QUICK_REFERENCE.txt

╚══════════════════════════════════════════════════════════════╝

